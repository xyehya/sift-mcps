.DS_Store
._.DS_Store
**/.venv/
**/__pycache__/
**/*.pyc
*.egg-info/
.venv/
uv.lock
.pytest_cache/
.ruff_cache/
dist/
build/
coverage/
.coverage
.coverage-reports/
.coverage-*.json
*.log
.env
.env.*
!.env.example
.mcp.json

# Host-local agent instructions. Keep untracked per workstation.
/AGENTS.md
/CLAUDE.md

# Evidence files — never commit real forensic evidence
*.E01
*.e01

# Code knowledge-graph artifacts (understand-anything) — local tooling, not source
.understand-anything/

# Playwright MCP render scratch (excalidraw export)
.playwright-mcp/
.agents/

# Machine-local skills lockfile (linear-cli skills) — not shared
skills-lock.json
.claude/settings.json
