::code-comment{title="[P0] Direct Gateway REST control-plane routes lack operator, origin, and re-auth gates" body="Raw Gateway REST mutation routes accept a generically authenticated identity and then mutate backend registry state, service lifecycle state, and join-code state without a route-local operator/admin decision, Origin protection, or recent re-authentication. Add a shared sensitive-action dependency for all raw control-plane mutation routes requiring operator/admin authority, same-origin or CSRF protection as applicable, and recent Supabase re-auth." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/rest.py" start=1027 end=1064 priority=0 confidence=0.9}
::code-comment{title="[P1] REST tool execution bypasses the MCP policy middleware stack" body="The legacy REST tool route calls Gateway.call_tool directly after blocking only agent/service principals. The MCP endpoint is the path that runs tool authorization, add-on authority, evidence gate, response guard, DB-first audit, and OpenSearch job dispatch. Remove the REST tool execution route or route it through the exact same policy decision stack as MCP before dispatch." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/rest.py" start=229 end=246 priority=1 confidence=0.9}
::code-comment{title="[P1] Examiner backend management can register and start arbitrary stdio backend commands" body="The backend management flow lets a non-readonly examiner register stdio backend metadata that includes command, args, cwd, and env refs, and later start or restart the backend. Require owner/admin or explicit backend-admin authority, restrict commands to signed installed add-ons or an allowlisted catalog, and launch add-ons under a minimized environment and sandbox." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/case-dashboard/src/case_dashboard/backends_routes.py" start=171 end=203 priority=1 confidence=0.65}
::code-comment{title="[P1] HTTP MCP backend runtime egress validation can reach private or rebound destinations" body="Remote manifest fetches have a private-address validator, but DB-registered HttpMCPBackend runtime URLs are later materialized and connected with only syntax checks. Apply one shared egress policy before persistence, during materialization, and immediately before every connection or reconnect, and attach credentials only after destination validation." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/backends/__init__.py" start=279 end=318 priority=1 confidence=0.9}
::code-comment{title="[P1] Privileged run_command sudo fallback bypasses runtime-user isolation" body="run_command validates the requested pipeline and denies caller-supplied sudo, but if a privileged allowlisted stage fails with a permission error it retries internally through sudo and clears per-stage runtime_user. Remove automatic sudo retry from agent-facing run_command or split it into an explicit operator-approved privileged tool, and never clear runtime_user silently." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-core/src/sift_core/execute/tools/generic.py" start=219 end=267 priority=1 confidence=0.9}
::code-comment{title="[P1] OpenSearch ingest privileged mount path runs outside the generic command validator and sandbox" body="Non-dry-run OpenSearch ingest is redirected out of the Gateway sandbox into a dedicated mount-capable worker that launches ingest_cli and direct sudo mount helpers on attacker-supplied evidence images. Treat mount ingest as a separate privileged broker with minimized sudoers, explicit operator approval, and VM or container isolation for hostile image bytes." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/policy_middleware.py" start=1123 end=1164 priority=1 confidence=0.9}
::code-comment{title="[P1] OpenSearch explicit index parameter overrides active-case isolation" body="Case-scoped OpenSearch query tools expose an explicit index parameter. Gateway injects or checks case_id and case_dir but not index, and the backend resolver returns caller-supplied index before active-case context. Remove arbitrary index from case-scoped tools or bind final resolved indexes to the DB-active case after all resolution." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/src/opensearch_mcp/registry.py" start=74 end=111 priority=1 confidence=0.9}
::code-comment{title="[P1] Legacy token lifecycle lets examiners mint or rotate broad mcp:* agent tokens" body="Legacy portal token endpoints allow any examiner-role principal to create, rotate, or reactivate service/agent tokens, and agent tokens are assigned broad mcp:* scope. Retire the legacy lifecycle or require owner/admin plus Supabase re-auth, replace mcp:* with least-privilege scopes, and bind tokens to purpose and case where possible." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/case-dashboard/src/case_dashboard/routes.py" start=3976 end=4090 priority=1 confidence=0.9}
::code-comment{title="[P1] Supabase-active auth accepts legacy token fallback by default across REST and MCP" body="When Supabase auth is enabled, legacy token fallback still defaults to true. Invalid or unavailable Supabase resolution can fall through to legacy identities, and MCP compatibility can stamp legacy identities with mcp:* scopes. Default fallback to false when Supabase is enabled and require an explicit time-bounded migration flag." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/supabase_auth.py" start=185 end=204 priority=1 confidence=0.9}
::code-comment{title="[P1] Registered stdio MCP backends inherit the full Gateway environment including service secrets" body="DB-registered stdio backends start with env = dict(os.environ), then overlay configured env refs. Any registered or compromised stdio backend can read unrelated Gateway process secrets such as DSNs, Supabase keys, and tokens. Start stdio backends from a minimal base environment and add only explicitly approved env refs." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/backends/stdio_backend.py" start=85 end=114 priority=1 confidence=0.9}
::code-comment{title="[P2] RAG URL fetch allowlist does not validate resolved destination addresses" body="RAG source fetching blocks IP literals and requires fixed allowlisted hostnames, but it never resolves those hostnames to reject private, loopback, link-local, reserved, or rebound addresses before urlopen connects. Resolve and classify destinations before connect and enforce the same rule at the actual transport." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/forensic-rag-mcp/src/rag_mcp/sources.py" start=329 end=353 priority=2 confidence=0.65}
::code-comment{title="[P2] OpenSearch tar archive ingest validates containment only after extraction" body="Tar evidence archives are extracted with system tar before application-level containment validation runs. Preflight tar members before extraction, reject absolute paths, dot-dot, symlinks, hardlinks, devices, FIFOs, special files, ownership restoration, and unusual modes, then post-check every extracted entry." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/src/opensearch_mcp/ingest_cli.py" start=719 end=738 priority=2 confidence=0.65}
::code-comment{title="[P2] OpenSearch memory ingest has a separate unchecked 7z extraction path" body="Memory ingest handles zip and 7z inputs through its own 7z extraction path, separate from generic archive handling. It does not preflight members or validate resolved extracted files before selecting a memory image for Volatility. Route memory archives through the shared safe extractor or add equivalent preflight and post-checks here." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/src/opensearch_mcp/server.py" start=3745 end=3925 priority=2 confidence=0.65}
::code-comment{title="[P2] OpenSearch status exposes all case index names and counts without active-case binding" body="opensearch_status and related resources enumerate every case-* index name, document count, size, and status without active-case filtering. Separate cluster health from index catalog data, filter catalog data to the active case, or require explicit admin/operator scope for all-case index metadata." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/src/opensearch_mcp/server.py" start=1359 end=1382 priority=2 confidence=0.9}
::code-comment{title="[P2] OpenSearch enrichment direct-MCP scope fallback fails open when unset" body="Gateway MCP enforces enrichment:intel, but the backend fallback denies only when SIFT_ENRICHMENT_SCOPE is nonempty and wrong. If unset, dry_run=false starts enrichment in direct-backend or Gateway-bypass paths. For dry_run=false, deny when SIFT_ENRICHMENT_SCOPE is unset or missing enrichment:intel or wildcard." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/sift-backend.json" start=448 end=484 priority=2 confidence=0.9}
::code-comment{title="[P2] evidence_unseal SECURITY DEFINER RPC omits explicit PUBLIC execute revoke" body="A later migration adds app.evidence_unseal as SECURITY DEFINER and grants execute to service_role, but does not explicitly revoke the PostgreSQL default EXECUTE privilege from PUBLIC. Add an idempotent migration revoking EXECUTE from PUBLIC and granting only service_role, plus a hardening test for all app SECURITY DEFINER functions." file="/home/yk/AI/SIFTHACK/sift-mcps/supabase/migrations/202606160100_evidence_unseal.sql" start=59 end=69 priority=2 confidence=0.9}
::code-comment{title="[P2] OpenCTI optional stack reuses credentials and disables datastore hardening" body="The OpenCTI optional compose stack reuses OPENCTI_ADMIN_TOKEN across OpenCTI admin/API, RabbitMQ, MinIO, worker, and connectors, uses mutable latest images, and disables the OpenSearch security plugin inside the OpenCTI network. Split secrets by component, pin images by digest, and enable datastore auth where broader network access exists." file="/home/yk/AI/SIFTHACK/sift-mcps/docker-compose.opencti.yml" start=19 end=38 priority=2 confidence=0.9}
::code-comment{title="[P2] OpenSearch generic 7z/zip archive extraction lacks application containment checks" body="Generic zip and 7z evidence archives are extracted by external 7z into a case temp directory without application-level member preflight, post-extraction containment validation, or warning-class inspection. List and validate archive entries before extraction and treat dangerous warnings as failure before downstream parsing or mounting." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opensearch-mcp/src/opensearch_mcp/containers.py" start=23 end=33 priority=2 confidence=0.65}
::code-comment{title="[P2] OpenCTI configuration permits remote plaintext HTTP token transport" body="OpenCTI MCP accepts non-local http URLs with only warnings, then constructs the OpenCTI API client with the API token for that plaintext endpoint. Reject non-loopback http OpenCTI URLs by default, require an explicit unsafe development override, and enforce HTTPS with certificate validation for remote endpoints." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/opencti-mcp/src/opencti_mcp/config.py" start=144 end=183 priority=2 confidence=0.9}
::code-comment{title="[P2] Public Wintools join can persist attacker-selected HTTP backend URLs after join-code exchange" body="The public setup join route treats the one-time join code as the credential. For machine_type=wintools, it accepts a caller-supplied HTTP URL and token, stores the token in process env, builds an HTTP backend config, and calls register_backend_logic. Bind join codes to expected host identity and re-validate runtime egress on every connect." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-gateway/src/sift_gateway/rest.py" start=651 end=680 priority=2 confidence=0.65}
::code-comment{title="[P2] RAG URL fetch validates redirects only after urllib has followed them" body="RAG fetch validates the original URL, then calls urllib urlopen, whose default redirect handling connects to the redirected target before response.geturl is inspected and final URL validation runs. Disable automatic redirects and implement manual redirect handling that validates scheme, hostname, and resolved IP before each next request." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/forensic-rag-mcp/src/rag_mcp/sources.py" start=329 end=353 priority=2 confidence=0.9}
::code-comment{title="[P2] run_command systemd auto mode silently downgrades cgroup and network isolation" body="When SIFT_EXECUTE_SYSTEMD_SCOPE=auto and systemd-run is unavailable, the executor logs a warning and runs the direct worker. That silently loses systemd cgroup properties including IPAddressDeny=any. Use fail-closed systemd scope mode for production and surface isolation status in tool results and audit." file="/home/yk/AI/SIFTHACK/sift-mcps/packages/sift-core/src/sift_core/execute/executor.py" start=49 end=58 priority=2 confidence=0.9}
