# Snapshot volume mount for SIFT backup/restore
# Optional local snapshot-volume override; do not modify the main compose file.
services:
  opensearch:
    volumes:
      - /var/lib/sift/snapshots:/usr/share/opensearch/snapshots
