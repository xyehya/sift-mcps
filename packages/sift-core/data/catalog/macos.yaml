category: macos

tools:
  - name: mac-apt
    binary: mac_apt.py
    input_style: positional
    output_format: text
    timeout_seconds: 1800
    interactive: false
    description: "macOS/iOS artifact parsing framework (read-only). Plugin-driven positional usage: <input_type> <input_path> <plugin...>; run with -h to list input types and plugins."
    fk_tool_name: mac_apt
