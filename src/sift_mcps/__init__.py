"""Meta-package for the SIFT MCP workspace.

Runtime implementations remain in the workspace member distributions.  This
package exists so the published root extras (`core`, `rag`, `windows-triage`,
`core-addons`, and `opencti`) have a buildable, registry-installable anchor.
"""

